module.exports = {
  dependencies: {
    "react-native-siri-shortcut": {
      platforms: {
        ios: null // disable autolinking as it has some issues for now
      }
    },
    "react-native-unimodules": {
      platforms: {
        ios: null // disable autolinking as it has some issues for now
      }
    },
    "expo-speech": {
      platforms: {
        ios: null // disable autolinking as it has some issues for now
      }
    }
  }
};
